services:
  app:
    environment:
      APP_URL: "${SONGCHART_CODESPACES_APP_URL}"
      SESSION_DOMAIN: ""
      SESSION_SECURE_COOKIE: "true"
      SONGCHART_DEV_ADMIN_PASSWORD: "${SONGCHART_DEV_ADMIN_PASSWORD:-}"
    ports:
      - "127.0.0.1:8000:8000"
