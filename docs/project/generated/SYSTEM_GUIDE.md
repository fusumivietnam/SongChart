# SongChart System Guide

> Generated orientation layer. Source authorities are linked in `system-knowledge.json`; do not edit this file manually.

## 1. What this system is

Music discovery, canonical metadata, provenance, provider evidence and legal listening or viewing destinations.

The short mental model is: **user journey -> capability/use case -> route/implementation -> domain/data owner -> verification -> roadmap**.

A route or UI surface may already exist even when its executable use-case contract is not mapped yet. This guide reports those as **implemented surface / contract gap**, never as “feature missing”.

## 2. What exists now

- **visitor.discover** — `implemented` — Find canonical music entities and navigate trustworthy public detail pages. (routes 0, mapped use cases 15, contract gaps 0)
- **visitor.follow_destination** — `partial` — Move from a canonical SongChart entity to an approved listening/watching destination while retaining canonical context. (routes 0, mapped use cases 5, contract gaps 0)
- **member.manage_account** — `partial` — Manage account identity and security without coupling account data to provider identities. (routes 3, mapped use cases 0, contract gaps 3)
- **editor.ingest_and_admit** — `implemented` — Discover/import provider evidence, resolve identity, review conflicts, and admit provider-neutral canonical data. (routes 4, mapped use cases 0, contract gaps 4)
- **operator.observe_and_recover** — `implemented` — Observe provider/import/runtime state and recover bounded failures without bypassing canonical admission rules. (routes 1, mapped use cases 0, contract gaps 1)

## 3. Coverage at a glance

- Journeys: `5`
- Executable use-case contracts: `33`
- Journey-linked use-case contracts: `20`
- Implemented journey route surfaces: `8`
- Implemented surfaces missing executable use-case mapping: `8`
- Unknown declared entrypoints: `0`
- Current stage: `22.0` — System Control Plane & Pre-Data Stabilization (`implementing`)

## 4. How to trace one feature

1. Start from a journey ID in `system-knowledge.json`.
2. Check `implemented_route_surfaces` to prove the HTTP surface actually exists in Laravel.
3. Follow `mapped_use_cases`; each mapped contract carries route, implementation and data surfaces when known.
4. If the surface exists in `contract_gaps`, do not infer its application owner from controller/function names. Close the contract mapping first.
5. Follow data-surface owners into schema/domain authority instead of searching table names blindly.
6. Check current stage/roadmap before adding a new capability.

## 5. Mapping debt that should not be guessed around

- `member.manage_account` -> `account.overview`: Laravel proves `GET|HEAD /account` exists; executable use-case contract is still missing.
- `member.manage_account` -> `account.profile`: Laravel proves `GET|HEAD /account/profile` exists; executable use-case contract is still missing.
- `member.manage_account` -> `account.security`: Laravel proves `GET|HEAD /account/security` exists; executable use-case contract is still missing.
- `editor.ingest_and_admit` -> `admin.canonical-admissions.index`: Laravel proves `GET|HEAD /admin/canonical-admissions` exists; executable use-case contract is still missing.
- `editor.ingest_and_admit` -> `admin.canonical-admissions.show`: Laravel proves `GET|HEAD /admin/canonical-admissions/{admission}` exists; executable use-case contract is still missing.
- `editor.ingest_and_admit` -> `admin.canonical-admissions.stage`: Laravel proves `POST /admin/canonical-admissions/assertions/{assertion}` exists; executable use-case contract is still missing.
- `editor.ingest_and_admit` -> `admin.canonical-admissions.decide`: Laravel proves `POST /admin/canonical-admissions/{admission}/decisions` exists; executable use-case contract is still missing.
- `operator.observe_and_recover` -> `admin.audit.index`: Laravel proves `GET|HEAD /admin/audit` exists; executable use-case contract is still missing.

## 6. Audience projections

- **New developer:** read this guide, then `FEATURE_MATRIX.md`, then open only authorities referenced by the feature being changed.
- **AI:** load `system-knowledge.json` first; resolve semantic IDs, mapping state and source authorities before inspecting implementation files.
- **Product owner:** focus on journey state, product/domain gaps, decision debt and roadmap; contract mapping debt is engineering detail.
- **Operator:** follow operational journeys and verification/recovery authorities.
- **End user:** future guides should be generated only from implemented public capabilities and must hide internal IDs, contracts and infrastructure.

## 7. Rule for future documentation

Do not create an independently maintained user/dev/AI guide that copies system facts. Correct the owning authority or mapping, then regenerate the appropriate audience projection from this knowledge layer.
